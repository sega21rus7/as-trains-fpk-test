[mcp_servers.playwright]
command = "npx"
args = [
  "--no-install",
  "playwright-mcp",
  "--headless",
  "--isolated",
  "--output-dir",
  "output/playwright",
]
enabled = true
required = false
startup_timeout_sec = 30
tool_timeout_sec = 60
